# react-ghaction
React application with build with GH Actions and publishes to GH Pages.

# directory structure
```
.
└── README.md
    └── .github
        └── workflows
            └── react-build.yaml
└── package.json
```